# Railway sets PORT automatically. For local Docker/production:
PORT=8787

# Bind address (optional; defaults to 0.0.0.0 when PORT is set by Railway)
# HOST=0.0.0.0

# Trust reverse-proxy headers (Railway load balancer)
FORWARDED_ALLOW_IPS=*
